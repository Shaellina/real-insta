<h1>Post Edit</h1>
<%= form_tag(post_path(@post.id), method: "put") do %>
    Image : <%= text_field_tag :image_url, "#{@post.image_url}" %><br>
    Story : <%= text_field_tag :story, @post.story %><br>
    <%= submit_tag("Edit") %>
<% end %>